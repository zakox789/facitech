import 'package:flutter/material.dart';

class CloseTicket extends StatelessWidget {
  const CloseTicket({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('CloseTicket'),
    );
  }
}
